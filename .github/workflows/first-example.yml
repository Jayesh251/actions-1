name: my first workflow

on: push

jobs:
    first-job:
     runs-on: ubuntu-latest
     steps:
        - name: Welcome Message
          run: echo "My first Github Actions Job"

        - name: list the files
          run: ls -la

        - name: Read Files
          run: cat README.md  

